{% if section.settings.display_product_detail_customtab %}
    <div class="custom-product-description">
        <div class="container">            
            <div class="product-description-content row">
                <div class="product-img col-md-6">
                    {%- assign img_pr = product.featured_image.src | product_img_url: '800x800' -%}

                    <img src="{{ product.featured_image.src | product_img_url: '300x' }}" alt="{{ product.featured_image.alt | escape }}"             
                       data-src="https:{{ img_pr }}"
                       class="lazyload"
                       data-widths="[180, 360, 540, 720, 900, 1080, 1296, 1512, 1728, 2048]"
                       data-aspectratio="{{ product.featured_image.aspect_ratio }}"
                       data-sizes="auto">
                </div>
                <div class="product-tab col-md-6">
                    {% if section.settings.display_product_custom_tab1 == false and section.settings.display_product_custom_tab2 == true %}
                        {% assign tab2_active = true %}
                    {% endif %}

                    {% if section.settings.product_customtab_title %}
                        <div class="widget-title">   
                            <h3 class="box-title">
                              <span class="title"> 
                                {% render 'multilang' with section.settings.product_customtab_title%}
                              </span>
                            </h3>    
                        </div>
                    {% endif %}

                    {% if section.settings.display_product_custom_tab1 or section.settings.display_product_custom_tab2 %}
                        <ul class="list-tabs">
                            {% if section.settings.display_product_custom_tab1 and section.settings.product_custom_tab_title1 != blank %}
                                <li class="tab-title">
                                    <a href="#" data-tapTop data-target="#collapse-tab-custom1" class="tab-links active">
                                        {% render 'multilang' with section.settings.product_custom_tab_title1 %}
                                    </a>
                                </li>
                            {% endif %}

                            {% if section.settings.display_product_custom_tab2 and section.settings.product_custom_tab_title2 != blank %}
                                <li class="tab-title">
                                    <a href="#" data-tapTop data-target="#collapse-tab-custom2" class="tab-links{% if tab2_active %} active{% endif %}">
                                        {% render 'multilang' with section.settings.product_custom_tab_title2 %}
                                    </a>
                                </li>
                            {% endif %}
                        </ul>
                    {% endif %}

                    {% if section.settings.display_all_custom_tab1 %}
                            <div class="tab-content active" data-TabContent id="collapse-tab-custom1">
                                {% if product.description contains '[custom tab1]' %}
                                    {% assign des = section.settings.custom_tab_1_all_content %}

                                    {% if settings.enable_multilang and des contains '[lang2]' %}
                                        <div class="lang1">
                                            {{ des | split: '[lang2]' | first }}
                                        </div>
                                        <div class="lang2">
                                            {{ des | split: '[lang2]' | last }}
                                        </div>
                                    {% else %}
                                        <div>
                                            {{ des | split: '[lang2]' | first }}
                                        </div>
                                    {% endif %}
                                {% endif %}
                            </div>
                    {% else %}
                        {% if section.settings.display_product_custom_tab1 %}
                            <div class="tab-content active" data-TabContent id="collapse-tab-custom1">
                                {% if product.description contains '[custom tab1]' %}
                                    {% assign des = product.description | split:'[custom tab1]' | last | split: '[/custom tab1]' | first %}

                                    {% if settings.enable_multilang and des contains '[lang2]' %}
                                        <div class="lang1">
                                            {{ des | split: '[lang2]' | first }}
                                        </div>
                                        <div class="lang2">
                                            {{ des | split: '[lang2]' | last }}
                                        </div>
                                    {% else %}
                                        <div>
                                            {{ des | split: '[lang2]' | first }}
                                        </div>
                                    {% endif %}
                                {% endif %}
                            </div>
                        {% endif %}
                    {% endif %}

                    {% if section.settings.display_all_custom_tab2 %}
                            <div class="tab-content{% if tab2_active %} active{% endif %}" data-TabContent id="collapse-tab-custom2">
                                {% if product.description contains '[custom tab2]' %}
                                    {% assign des = section.settings.custom_tab_2_all_content %}

                                    {% if settings.enable_multilang and des contains '[lang2]' %}
                                        <div class="lang1">
                                            {{ des | split: '[lang2]' | first }}
                                        </div>
                                        <div class="lang2">
                                            {{ des | split: '[lang2]' | last }}
                                        </div>
                                    {% else %}
                                        <div>
                                            {{ des | split: '[lang2]' | first }}
                                        </div>
                                    {% endif %}
                                {% endif %}
                            </div>
                    {% else %}
                        {% if section.settings.display_product_custom_tab2 %}
                            <div class="tab-content{% if tab2_active %} active{% endif %}" data-TabContent id="collapse-tab-custom2">
                                {% if product.description contains '[custom tab2]' %}
                                    {% assign des = product.description | split:'[custom tab2]' | last | split: '[/custom tab2]' | first %}

                                    {% if settings.enable_multilang and des contains '[lang2]' %}
                                        <div class="lang1">
                                            {{ des | split: '[lang2]' | first }}
                                        </div>
                                        <div class="lang2">
                                            {{ des | split: '[lang2]' | last }}
                                        </div>
                                    {% else %}
                                        <div>
                                            {{ des | split: '[lang2]' | first }}
                                        </div>
                                    {% endif %}
                                {% endif %}
                            </div>
                        {% endif %}
                    {% endif %}

                </div>
            </div>
        </div>
    </div>
{% endif %}